/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package basicjava;

/**
 *
 * @author kevinestrella
 */
public class StringMethods {
    public static void main(String [] args)
    {
        String name = "Bro";
     //returns boolean and checks if they match the name variable and whats inside(case sensitive
     //  Boolean result = name.equals("Bro");
     // or
     //  boolean result2 = name.equalsIgnoreCase("Bro"); // not case sensitive
    
     // returns the length
     //   int result = name.length();
     
     //character at a certain position
     //char result = name.charAt(0);
     
     //finds char and returns the index of it
     //int result = name.indexOf("o");
     
     //checks if empty returns true or false
     //boolean result = name.isEmpty();
     
     //makes everything to upper case
     //String result = name.toUpperCase();
     
     //makes everything to lower case
     //String result = name.toLowerCase();
     
     //removes empty space befor and after characters
     //String result = name.trim();
     
     //replaces characters in a string(this replaces all o's with a's
     //String result = name.replace('o','a');
        System.out.println(result);
    }
    
}
